<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="csxt.jyx.dao.SGatherDetailsMapper">
<!--    添加-->
    <insert id="addSGatherDetails" parameterType="csxt.entity.SGatherDetails">
        insert into s_gather_details values(null,#{parentId},#{productId},#{productName},#{productDescribe},#{amount},#{amountUnit},#{costPrice},#{subtotal},#{gatheredAmount},#{gatherTag})
    </insert>
</mapper>